import { type ActivitiesOptions, ActivityType } from 'discord.js';

export const activities: ActivitiesOptions[] = [
	{
		name: 'LunarBot',
		type: ActivityType.Watching
	}
];

export const ownerId = '883105651407077386';
